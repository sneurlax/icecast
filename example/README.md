# Icecast example

Run as in
```
dart run icecast_example.dart
```

Compile as in 
```
dart compile exe --target-os macos
                             linux
                             windows
                             etc
```
